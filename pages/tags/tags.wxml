<view class="tags">
  <block wx:for="{{tags}}">
    <view class="tag-wrap" bindtap="toTag" data-id="{{item.id}}" data-name="{{item.name}}">
      <image class="tag-img" src="{{item.feature_image}}" mode="aspectFill" />
      <view class="tag-name">{{item.name}}</view>
      <view class="tag-description ellipsis">{{item.description}}</view>
    </view>
  </block>
  <copyright />
</view>
