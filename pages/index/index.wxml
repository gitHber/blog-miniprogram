<!--index.wxml-->
<scroll-view class="container" enable-back-to-top scroll-y bindscrolltolower="loadMore" lower-threshold="200">
  <carousel list="{{posts_3}}"></carousel>
  <view class="search-wrap">
    <search bindsearch="onSearch"/>
  </view>
   <block wx:for="{{posts}}" wx:key="{{index}}">
    <post postData="{{item}}"/>
  </block>
  <copyright />
</scroll-view>
 